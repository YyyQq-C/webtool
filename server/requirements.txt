# 图片工具后端 Python 依赖

# Web 框架与服务器
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
slowapi>=0.1.8

# 网页抓取与 PDF
playwright>=1.35.0
httpx>=0.24.0

# 去背景与图像处理
rembg>=2.0.0
Pillow>=10.0.0

# 去水印 (OpenCV)
opencv-python-headless>=4.8.0
numpy>=1.24.0

# PDF转Word (兜底方案)
pdf2docx>=0.5.0

# OCR 文字识别
pytesseract>=0.3.10
# PaddleOCR (可选，准确度更高)
# paddleocr>=2.6.0
# EasyOCR (可选)
# easyocr>=1.6.0

# 文档生成
openpyxl>=3.0.0
python-docx>=0.8.11